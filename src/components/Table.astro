---
interface Props {
  headers: string[];
  data: (string | number)[][];
  variant?: 'default' | 'compact' | 'striped';
}

const { headers, data, variant = 'default' } = Astro.props;
---

<div class={`table-wrapper ${variant}`}>
  <table class="modern-table">
    <thead>
      <tr>
        {headers.map(header => (
          <th>{header}</th>
        ))}
      </tr>
    </thead>
    <tbody>
      {data.map((row, index) => (
        <tr>
          {row.map((cell, cellIndex) => (
            <td class={cellIndex === 0 ? 'first-col' : ''}>{cell}</td>
          ))}
        </tr>
      ))}
    </tbody>
  </table>
</div>